<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Pressed State -->
    <item android:state_pressed="true">
        <shape>
            <solid android:color="#e4e3ff" /> <!-- Color when pressed -->
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="#c4c4c4" />
        </shape>
    </item>

    <!-- Focused State -->
    <item android:state_focused="true">
        <shape>
            <solid android:color="#e4e3ff" /> <!-- Color when focused -->
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="#3100d4" />
        </shape>
    </item>

    <!-- Default State -->
    <item>
        <shape>
            <solid android:color="#FFFFFF" /> <!-- Default color -->
            <corners android:radius="12dp" />
            <stroke android:width="1dp" android:color="#3100d4" />
        </shape>
    </item>

</selector>
